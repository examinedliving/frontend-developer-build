// wrap it all in a function, because I don't need any of it to be accessible globally
(function(w, d, undefined) {
  // @include ./source/transforms.js
  // @include ./source/dg.js
}(window, document));